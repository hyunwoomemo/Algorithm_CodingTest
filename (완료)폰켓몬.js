/* function solution(nums) {
  return nums.filter((v, i, arr) => arr.indexOf(v) === i).length >= nums.length / 2 ? nums.length / 2 : nums.filter((v, i, arr) => arr.indexOf(v) === i).length;
} */

// 맞아요!!!!!!!!!!!

solution([3,3,1,2]);